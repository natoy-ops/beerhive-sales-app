/**
 * Application Version Constants
 *
 * Centralized, single source of truth for app versioning.
 * Update `APP_VERSION` for each release. This is used by UI badges
 * and any diagnostics or health endpoints that want to report version.
 */
export const APP_NAME = 'BeerHive POS';

/**
 * Semantic application version.
 * Visible on the login page and the sidebar.
 */
export const APP_VERSION = '1.1.3';
